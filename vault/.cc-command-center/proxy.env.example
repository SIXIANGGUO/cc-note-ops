# 可选代理配置
#
# 默认不启用。需要让按钮后台调用 Claude Code 时使用你的本机代理，
# 请复制本文件为 proxy.env，并按自己的代理端口修改。
#
# 示例：
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890
# ALL_PROXY=socks5://127.0.0.1:7890
# NO_PROXY=localhost,127.0.0.1
#
# 注意：
# 1. 这里不会保证账号安全，也不会绕过服务条款。
# 2. 它只负责把代理环境变量显式传给后台 claude 命令。
# 3. 不要把 token、cookie、账号密码写进这个文件。
